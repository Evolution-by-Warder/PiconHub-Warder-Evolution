#!/bin/sh
# PiconHub Warder Evolution pre-install safety policy.
# DO NOT remove ChocholousekPicons here. The new package must be unpacked and
# its settings migration verified before legacy cleanup is allowed.
set -e
exit 0
