# IPK packaging

Packaging metadata is kept separately from the runtime tree. The package currently retains the original package identity `enigma2-plugin-extensions-chocholousek-picons` so installation upgrades the existing plugin instead of creating a parallel copy.

Current package generation: `5.0.240904.2`.
