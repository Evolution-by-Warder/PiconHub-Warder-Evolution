# PiconHub plugin updates

Plugin updates are independent from picon/database updates.

The update manifest must provide at minimum the plugin version, package location, SHA-256 checksum and changelog. After a successful plugin update, the Enigma2 GUI restarts automatically. The user is warned before the update starts.
